/** Max raw upload per listing photo (bytes). */
export const LISTING_PHOTO_MAX_BYTES = 10 * 1024 * 1024

/** Max photos per listing. */
export const LISTING_PHOTO_MAX_COUNT = 30

/** Max review photos per review. */
export const REVIEW_PHOTO_MAX_COUNT = 8

export const REVIEW_PHOTO_MAX_BYTES = 8 * 1024 * 1024

/** Max listing document size (bytes). */
export const LISTING_DOCUMENT_MAX_BYTES = 15 * 1024 * 1024

export const LISTING_DOCUMENT_MAX_COUNT = 10

/** Max files per ownership claim request. */
export const LISTING_CLAIM_ATTACHMENT_MAX_COUNT = 5

/** Max size per claim attachment (bytes). */
export const LISTING_CLAIM_ATTACHMENT_MAX_BYTES = 15 * 1024 * 1024

/** Max story image upload (bytes). */
export const STORY_IMAGE_MAX_BYTES = 10 * 1024 * 1024

/** Max story video upload (bytes). */
export const STORY_VIDEO_MAX_BYTES = 50 * 1024 * 1024

/** Max gallery items per listing news post. */
export const LISTING_NEWS_GALLERY_MAX = 5

export const LISTING_NEWS_PREVIEW_MAX_BYTES = 8 * 1024 * 1024

export const LISTING_NEWS_MEDIA_MAX_BYTES = 50 * 1024 * 1024
